type User {
    joinDate: String! #ToDo: update with timestamp type
    id: ID!
    email: String!
    username: String!
    recipes(limit: Int = 10, cursor: String): PaginatedRecipeResult!
}

type PaginatedRecipeResult implements PaginatedResult {
    items: [Recipe!]!
    pagination: PaginationInfo!
}
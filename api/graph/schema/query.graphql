type Query {
  user: UserQuery
  recipe: RecipeQuery
}

type UserQuery{
  byId(id: ID!) User
  bySlug(slug: String!): User
  byEmail(email: String!): User
} 

type RecipeQuery{
  byId(id: ID!) Recipe
  bySlug(slug: String!): Recipe
  list(limit: Int, nextCursor: String): PaginatedRecipes!
}
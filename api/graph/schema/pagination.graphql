interface PaginatedResult {
    pagination: PaginationInfo!
}

type PaginationInfo {
    count: Info!
    nextCursor: String
    prevCursor: String
}

type PaginatedRecipes implements PaginatedResult { 
    recipes: [Recipe!]!
    pagination: PaginationInfo!
}
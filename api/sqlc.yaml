version: "2"
sql:
  - engine: "postgresql"
    queries: "../db/queries/"
    schema: "../db/migrations/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"
